/** Figma default circled-arrow slot — 20×20px display (1714:1261, 1714:1266). */
export const CTA_ARROW_CLASS = "size-5 shrink-0";

/** Smaller card-row CTAs (`ctaSize="card"`). */
export const CTA_CARD_ARROW_CLASS = "size-4 shrink-0";

/** Label ↔ arrow gap — Figma hero / pill CTAs use 14px (1714:1257 row gap 14px). */
export const CTA_GAP_CLASS = "gap-3.5";

/** Default horizontal inset on standard pills — Figma px-20 (5×4px). */
export const CTA_PX_DEFAULT = "px-5";

/** Shared SVG artboard — all `/button-icon-*.svg` assets use this viewBox so scale matches. */
export const CTA_ARROW_SVG_SIZE = 17;
